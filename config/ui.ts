/**
 * Central UI feature switches.
 *
 * SHOW_DROPDOWN_ADD_BUTTON controls the inline "Add New" (+) button inside
 * searchable form dropdowns rendered by ERPDynamicModalForm. It is the global
 * default for the form's `showAddButton` prop; a form that passes the prop
 * explicitly overrides it. The Alt+C create shortcut is unaffected.
 */
export const SHOW_DROPDOWN_ADD_BUTTON = false;
